{
  "scripts": {
    "init": "yarn clean && yarn install && npm-run-all init:*",
    "init:main": "cd main && yarn install",
    "init:vite": "cd react-demo && yarn install",
    "clean": "rimraf **/node_modules",
    "start": "run-p start:*",
    "start:main": "cd main && yarn start",
    "start:vite": "cd react-demo && yarn dev"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "rimraf": "^3.0.2"
  }
}